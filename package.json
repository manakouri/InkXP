{
  "name": "inkxp",
  "version": "1.0.0",
  "description": "InkXP App Functions",
  "scripts": {
    "build": "mkdir -p dist && cp index.html dist/ && cp metadata.json dist/"
  },
  "dependencies": {
    "@google/genai": "^0.14.0",
    "firebase-admin": "^12.1.0"
  }
}