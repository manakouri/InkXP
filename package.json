{
  "name": "inkxp",
  "version": "1.0.0",
  "description": "InkXP App Functions",
  "scripts": {
    "build": "mkdir -p dist && cp index.html dist/ && cp metadata.json dist/ && cp index.tsx dist/"
  },
  "dependencies": {
    "@google/genai": "^0.14.0"
  }
}
